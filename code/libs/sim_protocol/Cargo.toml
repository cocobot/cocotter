[package]
name = "sim_protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
bitflags = { version = "2.6", features = ["serde"] }
