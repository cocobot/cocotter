[package]
name = "cancaner"
version = "0.1.0"
edition = "2021"
description = "Shared CAN protocol for cocotter robots (picotter/sabotter)"

[dependencies]
embedded-can = "0.4"

[features]
default = []
std = []
