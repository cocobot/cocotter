[package]
name = "board-simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
flume = { version = "0.11", default-features = false }
embedded-hal = "1.0"
log = "0.4"
simple_logger = "5.0"
vlx = { path = "../../libs/vlx" }
